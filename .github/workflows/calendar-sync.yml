name: Calendar Sync

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Get repository
        uses: actions/checkout@v4

      - name: Install Python packages
        run: pip install icalendar

      - name: Download calendar
        env:
          CALENDAR_URL: ${{ secrets.CALENDAR_URL }}
        run: |
          URL="$CALENDAR_URL"
          if [[ "$URL" == webcal://* ]]; then
            URL="https://${URL#webcal://}"
          fi
          if [[ "$URL" != http://* && "$URL" != https://* ]]; then
            echo "Invalid calendar URL"
            exit 1
          fi
          curl -fL "$URL" -o calendar.ics
          echo "Calendar downloaded successfully"

      - name: Convert calendar to JSON
        run: |
          python - <<'PY'
          from icalendar import Calendar
          import json

          with open("calendar.ics", "rb") as f:
              cal = Calendar.from_ical(f.read())

          events = []

          for component in cal.walk():
              if component.name != "VEVENT":
                  continue

              start = component.get("DTSTART")
              end = component.get("DTEND")

              if not start:
                  continue

              start = start.dt
              end = end.dt if end else None

              events.append({
                  "summary": str(component.get("SUMMARY", "")),
                  "location": str(component.get("LOCATION", "")),
                  "description": str(component.get("DESCRIPTION", "")),
                  "start": start.isoformat(),
                  "end": end.isoformat() if end else ""
              })

          events.sort(key=lambda x: x["start"])

          with open("schedule.json", "w") as f:
              json.dump(events, f, indent=2)

          print(f"Saved {len(events)} calendar events")
          PY

      - name: Commit updated schedule
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git rebase origin/main
          git add schedule.json
          git diff --cached --quiet || git commit -m "Update flight schedule"
          git push origin HEAD:main
